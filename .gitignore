.DS_Store

# Never commit real credentials — this repo is public.
# Generate locally at deploy time (see server-lock/README.md).
.htpasswd
server-lock/.htpasswd

# Access-code hashes: generated by CI from the NOMIMA_ACCESS_CODES secret on deploy,
# or created locally from hashes.example.js for testing. Never commit real ones.
hashes.js
